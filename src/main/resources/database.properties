# Bank Management System Database Configuration
# You can use MySQL or enable H2 embedded mode for zero setup.

# Database Mode: 'h2' or 'mysql'
db.mode=h2

# H2 Configuration (Embedded file-based database)
h2.url=jdbc:h2:./bms_db;DB_CLOSE_DELAY=-1;MODE=MySQL
h2.user=sa
h2.password=

# MySQL Configuration
mysql.url=jdbc:mysql://localhost:3306/bank?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
mysql.user=root
mysql.password=Dnyanesh@123
